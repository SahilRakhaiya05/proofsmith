# Demo

The current Loop Theater uses the explicit fixture run `ps_demo_local`. It demonstrates interaction and information design only. Do not present it as a GitHub, TestSprite, mutation, review, deployment, or production artifact.

A truthful demo recording must wait for a complete external run and then link the issue, commits, PR, preview, TestSprite failure and pass, reviewer, mutation challenge, human approval, production verification, and matching `LOOP.md` lines.
